name: Apply Stage 3 Roadmap
on:
  push:
    branches: [main]
    paths: ['.automation/stage3_roadmap.py']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - run: |
          python3 .automation/stage3_roadmap.py
          rm .automation/stage3_roadmap.py
          rm .github/workflows/apply-stage3-roadmap.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "mark Vulkan viewport stage complete"
          git push origin HEAD:main
