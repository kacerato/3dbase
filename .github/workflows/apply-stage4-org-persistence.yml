name: Apply Stage 4 Org Persistence
on:
  push:
    branches: [main]
    paths: ['.automation/stage4_org_persistence.py']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - run: |
          python3 .automation/stage4_org_persistence.py
          rm .automation/stage4_org_persistence.py
          rm .github/workflows/apply-stage4-org-persistence.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "persist collections and layers"
          git push origin HEAD:main
